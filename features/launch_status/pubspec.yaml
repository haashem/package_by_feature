name: launch_status
description: Space missions launch status package
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  freezed_annotation: ^1.0.0
  json_annotation: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: 2.1.5  
  freezed: ^1.0.0
  json_serializable: ^6.0.1

flutter:
  uses-material-design: true
